[project]
name = "blog-map"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bs4>=0.0.2",
    "duckdb>=1.1.3",
    "ipykernel>=6.29.5",
    "local-geocode>=0.0.2",
]
